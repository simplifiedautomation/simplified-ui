import { SaValueFormatterPipe } from './sa-value-formatter.pipe';

describe('SaValueFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new SaValueFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
