import { SaDateTimePipe } from './sa-date-time.pipe';

describe('SADateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new SaDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
