import { SaSpinnerDirective } from './sa-spinner.directive';

describe('SaSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new SaSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
